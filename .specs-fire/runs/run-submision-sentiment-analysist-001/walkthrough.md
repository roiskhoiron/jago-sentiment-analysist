---
run: run-submision-sentiment-analysist-001
work_item: experiment-matrix-design
intent: sentiment-analisis-ulasan-bank-indonesia
generated: 2026-05-26T10:08:00Z
mode: autopilot
---

# Implementation Walkthrough: Experiment Matrix Design

## Summary

Mendefinisikan matriks eksperimen untuk pipeline sentiment analysis dengan 5 variasi model (Logistic Regression, Linear SVM, IndoBERT) dan kombinasi ekstraksi fitur yang berbeda. Setiap eksperimen dilengkapi target metrik akurasi >=85%, F1-score, dan konfigurasi train/test split yang reproduktibel.

## Structure Overview

Tiga file utama dihasilkan: `docs/experiment-matrix.md` (tabel konfigurasi eksperimen dalam Markdown), `docs/experiment-config.yaml` (versi mesin-baca untuk reproduktibilitas), dan `scripts/test_experiment_matrix.py` (validasi otomatis). Struktur mengikuti konvensi direktori yang telah disetup pada work-item project-structure-proposal.

## Files Changed

### Created

| File | Purpose |
|------|---------|
| `docs/experiment-matrix.md` | Tabel konfigurasi eksperimen 5 model dengan feature extraction, split config, target akurasi & F1 |
| `docs/experiment-config.yaml` | Mesin-baca YAML config untuk semua eksperimen (vectorizer settings, preprocessing, targets) |
| `scripts/test_experiment_matrix.py` | Validasi script — 5 tests, all passing |

### Modified

| File | Changes |
|------|---------|
| `.specs-fire/runs/run-submision-sentiment-analysist-001/plan.md` | Appended experiment-matrix-design section |
| `.specs-fire/runs/run-submision-sentiment-analysist-001/test-report.md` | Appended experiment-matrix-design test results (5/5 tests passing) |

## Key Implementation Details

### 1. Experiment Configuration Design
Dirancang dengan 5 eksperimen yang mencakup kombinasi: model klasik (LR, SVM) dan deep learning (IndoBERT), dengan variasi TF-IDF standar/extended/stemmed serta Bag-of-Words. Setiap eksperimen memiliki target akurasi dan F1-score spesifik.

### 2. Reproducibility by Design
Semua eksperimen menggunakan `random_state=42`, stratified split, dan menyimpan artifact (model, vectorizer, tokenizer) di direktori terstruktur.

## Decisions Made

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Jumlah eksperimen | 5 (bukan minimal 3) | Memberikan margin untuk variasi fitur ekstraksi dan lebih robust terhadap edge cases |
| Model baseline | Logistic Regression | Simple, interpretable, fast inference — ideal sebagai baseline |
| Model deep learning | IndoBERT (fine-tuning) | Pre-trained untuk Bahasa Indonesia, highest expected accuracy (>90%) |
| Format config | YAML + Markdown | Markdown untuk human-readable, YAML untuk mesin-baca dan scripting |
| Split ratio | 80/20 (primary), 75/25 (EXP-04) | Standar ML practice; 75/25 untuk BoW variant yang lebih data-hungry |

## Deviations from Plan

None — implementation follows the original work item plan precisely.

## Dependencies Added

| Package | Why Needed |
|---------|------------|
| pyyaml | Parsing experiment-config.yaml dalam test script |

## How to Verify

1. **Run validation tests**

   ```bash
   python3 scripts/test_experiment_matrix.py
   ```

   Expected: All 5 tests pass.

2. **Validate YAML config**

   ```bash
   python3 -c "import yaml; c=yaml.safe_load(open('docs/experiment-config.yaml')); print(f'{len(c[\"experiments\"])} experiments')"
   ```

   Expected: 5 experiments.

3. **Verify experiment matrix table**

   ```bash
   grep -c "EXP-" docs/experiment-matrix.md
   ```

   Expected: >= 6 lines (header + 5 experiments + references).

## Test Coverage

- Tests added: 5 (test_experiment_matrix_exists, test_minimal_3_models, test_experiment_config_valid_yaml, test_model_selection_criteria, test_all_experiments_have_targets)
- Coverage: All acceptance criteria validated
- Status: passing

## Ready for Review

- [x] All acceptance criteria met
- [x] Tests passing (5/5)
- [x] No critical issues
- [x] Documentation updated
- [x] Developer notes captured

## Developer Notes

- Experiment matrix design is intentionally decoupled from implementation — the config files can be read directly by training notebooks without parsing Markdown.
- The 5 experiments cover a good spread: 2 variants of LR, 2 variants of SVM, and 1 deep learning model.
- For future additions, simply append to `docs/experiment-matrix.md` table and add corresponding entry in `experiment-config.yaml`.

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-submision-sentiment-analysist-001*
